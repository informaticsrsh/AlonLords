{
  "name": "empire-lords",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@empire/web",
    "build": "npm run build --workspace=@empire/web",
    "test": "npm run test --workspace=@empire/game-core",
    "lint": "npm run lint --workspace=@empire/web"
  }
}
